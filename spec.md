# Domain Traffic Inspector 仕様

## 目的

現在のタブから発生する通信をドメイン単位で可視化し、ユーザーが通信ポリシーを管理できるChromeサイドパネル拡張機能を提供する。

## スコープ

- 開いている全タブの通信先をタブ単位、ブラウザーセッション単位で集計し、UIには選択中のタブだけを表示
- 通常／厳格モード、ブロック機能の有効状態、全タブルールは全タブ共通
- サイト専用ポリシーは登録ドメイン単位で保存し、そのサイト内の通信だけに適用
- データ処理と保存はユーザーの端末内で完結

## 動作モード

### 通常モード

ユーザーが明示的にブロックしたドメインの通信を停止する。

### 厳格モード

メインフレームを含むすべての通信を既定で停止する。許可リストに登録したドメインへの通信だけを通過させる。

### サイト保護

対象サイト本体を自動許可し、未登録の依存先をそのサイト内だけで停止する。依存先は恒久許可／ブロックに加え、5分間だけ許可できる。一時許可はChrome Alarmsで失効させ、Service Worker再起動時にも期限を復元する。

## ドメインルールの優先順位

- ルールは指定ドメイン自身と、そのサブドメインに適用する
- 複数のルールが一致する場合は、ラベル数が多い、より具体的なドメインを優先する
- 例: `example.com` をブロックし、`api.example.com` を許可すると、API配下だけを通過させる
- 通常モードの許可ルールは、親ドメインのブロックに対する例外として利用できる
- 厳格モードの全体ブロックより、個別の許可／ブロックルールを優先する
- サイト専用ポリシーは全タブルールより優先する
- サイト専用ポリシー内では、一時許可、個別ルール、既定ブロックの順に優先する

## 主なデータ

- `tabData`: タブIDごとのドメイン別件数、リソース種別、メインドメイン
- `domainRules`: ドメインごとの `block` / `allow`、メモ、タグ
- `allowedDomains`: 厳格モードの例外
- `currentMode`: `normal` または `strict`
- `blockingEnabled`: DNRルールを適用するかどうか
- `sitePolicies`: 登録ドメインごとの有効状態、個別ルール、一時許可の期限

## 制約

- 広告、CDN、サードパーティ判定は補助的な推定であり、安全性を保証しない
- 表示する件数は観測リクエスト数であり、ルール一致件数ではない
- Chrome 114以降を対象とする
- Chrome 145以降はDNRの `topDomains` でサイト範囲を指定し、Chrome 114〜144は `initiatorDomains` による互換スコープを使う
- Chrome 114〜119の動的ルール上限を考慮し、同時に有効化できる全DNRルールは最大5,000件とする
