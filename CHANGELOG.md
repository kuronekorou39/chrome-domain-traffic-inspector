# Changelog

## Unreleased

### Added

- 閲覧サイト内だけで未登録通信を停止するサイト専用ポリシー
- 通信先を5分だけ通し、Chrome Alarmsで自動失効させる一時許可
- 全タブ／このサイトを切り替えるポリシービルダーと適用範囲タグ

### Changed

- Chrome 145以降では `topDomains`、Chrome 114〜144では `initiatorDomains` を使ってサイト範囲を適用
- DNRルール総数をChrome 114〜119互換の5,000件以内に明示的に制限

## 1.1.0 - 2026-08-03

### Changed

- 厳格モードを、メインページを含む全ドメインの既定ブロックと許可リスト方式に統一
- 親ドメインの方針を継承し、より具体的なサブドメインで上書きできる階層ポリシーへ変更
- 現在サイトの許可と、表示中ドメインの一括許可による許可リスト作成フローを追加
- サイドパネルの情報設計、コントラスト、密度、キーボード操作を改善
- 16pxでも判別しやすいシールド／通信アイコンへ更新
- Service Worker再起動後もタブ別集計をセッション内で復元
- バージョンタグを使うGitHub Releaseフローへ変更
- 全タブで行うローカル集計をプライバシーポリシーへ明記
- 冗長な `tabs` 権限を削除

### Fixed

- モード／ブロック状態変更後に行ボタンが古い状態で残る問題
- DNR更新失敗を成功として表示する問題
- 許可解除後にルールメタデータが残る問題
- グローバル停止中に新しいブロックルールが有効化される問題
- 不正なインポートデータによる状態破損と描画エラー
- DNR更新失敗時に許可・ルール・モードの部分適用が残る問題
- 親ドメインルールがサブドメインのUI状態へ反映されない問題
- メモ・タグ付きブロックを解除すると再起動後に再適用される問題

### Security

- 不要な権限を削除
- ドメイン、タグ、メモ、インポートデータの検証を強化
- 開発依存関係を更新
