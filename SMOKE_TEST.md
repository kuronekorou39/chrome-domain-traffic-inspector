# Release smoke test

リリース候補をChromeへ「パッケージ化されていない拡張機能」として読み込み、以下を確認します。

## 基本動作

- サイドパネルが開き、選択中タブのドメインと件数が更新される
- タブを切り替えると表示対象も切り替わる
- 通常モードで個別ブロック／解除が反映される
- ブロック全体をOFFにすると通信が通り、ONへ戻すと保存済みルールが復元される

## 厳格モード

- 許可ルール0件で新しいHTTP/HTTPSサイトを開くと `ERR_BLOCKED_BY_CLIENT` になる
- 「現在サイトを許可」から停止したサイトを許可し、再読み込みすると表示される
- 検索・フィルター後の「表示中を許可」で対象ドメインだけが一括許可される
- 許可を解除して再読み込みすると再び停止する
- ファーストパーティとサードパーティの両方が未許可なら停止する
- `example.com` をブロックして `api.example.com` を許可すると、子ドメインの許可が優先される
- `example.com` を許可して `private.example.com` をブロックすると、子ドメインのブロックが優先される
- 親ルールが適用された行に「親ルール」と継承元の説明が表示される

## サイト保護

- ポリシー範囲を「このサイト」へ切り替え、保護を開始するとサイト本体は表示できる
- 未登録の外部依存先は対象サイト内で停止するが、別サイト上の同じ依存先には影響しない
- 通信行の許可／ブロックがサイト専用ルールとして反映され、全タブルールは変化しない
- 時計ボタンで許可した依存先が5分後に自動で再停止する
- 一時許可中にService Workerとサイドパネルを閉じても、再起動後に残り時間または期限切れが正しく反映される
- Chrome 145以降では通常スコープ、Chrome 114〜144では「互換スコープ」と表示される
- サイト保護を解除すると通信が通り、再度有効にすると保存済みサイトルールが復元される

## 永続化とルール管理

- 拡張機能のService Workerを停止／再起動しても設定とセッション中の集計が復元される
- ルールの追加、編集、タグ付け、個別削除、一括削除が反映される
- JSONエクスポート後、ルールを削除してインポートすると元の内容へ戻る
- 壊れたJSON、無効なドメイン、上限超過のメモ／タグが拒否される

## 表示

- ライト／ダークテーマで文字とボタンが判別できる
- 狭いサイドパネルでも横スクロールやボタンの欠落がない
- Tab、矢印、Enter、Escapeで主要操作ができ、フォーカス位置を確認できる

Chromeのバージョン、OS、確認した拡張機能バージョン、結果をリリースノートへ記録します。
