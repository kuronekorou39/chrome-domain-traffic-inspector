# Release smoke test

リリース候補をChromeへ「パッケージ化されていない拡張機能」として読み込み、以下を確認します。

## 基本動作

- サイドパネルが開き、選択中タブのドメインと件数が更新される
- タブを切り替えると表示対象も切り替わる
- 通常モードで個別ブロック／解除が反映される
- ブロック全体をOFFにすると通信が通り、ONへ戻すと保存済みルールが復元される

## 厳格モード

- 許可ルール0件で新しいHTTP/HTTPSサイトを開くと `ERR_BLOCKED_BY_CLIENT` になる
- 停止したサイトのドメインを許可して再読み込みすると表示される
- 許可を解除して再読み込みすると再び停止する
- ファーストパーティとサードパーティの両方が未許可なら停止する

## 永続化とルール管理

- 拡張機能のService Workerを停止／再起動しても設定とセッション中の集計が復元される
- ルールの追加、編集、タグ付け、個別削除、一括削除が反映される
- JSONエクスポート後、ルールを削除してインポートすると元の内容へ戻る
- 壊れたJSON、無効なドメイン、上限超過のメモ／タグが拒否される

## 表示

- ライト／ダークテーマで文字とボタンが判別できる
- 狭いサイドパネルでも横スクロールやボタンの欠落がない
- Tab、矢印、Enter、Escapeで主要操作ができ、フォーカス位置を確認できる

Chromeのバージョン、OS、確認した拡張機能バージョン、結果をリリースノートへ記録します。
